taset D2 – Ranking FIFA (histórico)

**Fuente / Origen:** football-ranking.com (ranking histórico FIFA)  
**Tipo de dato:** Tabular (extraído de tabla HTML a CSV)  
**Formato RAW esperado:** CSV sin transformaciones (tal cual se extrajo)  
**Fecha de extracción:** YYYY-MM-DD (poner fecha real)  
**Objetivo en el TFM:** Variable explicativa (proxy de fortaleza relativa de selección)

## Contenido esperado en esta carpeta
- `fifa_ranking_raw_<YYYY-MM-DD>.csv` (resultado del scraping/export)
- `source_url.txt` (URL exacta utilizada)
- `metadata.json` (metadatos de trazabilidad: fecha, método, notas)

## Notas
RAW debe mantenerse sin limpieza ni estandarización.  
La limpieza mínima y normalización se realiza en /data/processed (Persona 1).
